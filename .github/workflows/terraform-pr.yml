name: Terraform PR

# ── How AWS auth works ────────────────────────────────────────────────────────
# GitHub Actions uses OIDC (no static credentials stored anywhere):
#   1. Each job requests a short-lived OIDC token from GitHub.
#   2. aws-actions/configure-aws-credentials exchanges it with AWS STS for
#      temporary credentials, assuming the role in AWS_CI_ROLE_ARN.
#   3. The IAM role trust policy allows token.actions.githubusercontent.com
#      with sub: repo:rentlora/*:* — scoped to this org only.
#   Permissions required in the job: id-token: write
# ─────────────────────────────────────────────────────────────────────────────
#
# This workflow runs ONLY on pull requests. It validates, scans, and plans every
# stack so the reviewer sees the full diff before merge. It never touches AWS state
# (plan is read-only). Apply happens in terraform-deploy.yml after merge.

on:
  pull_request:
    branches: [main]

permissions:
  id-token: write          # required for OIDC → AWS STS
  contents: read
  pull-requests: write
  security-events: write

# One run per PR; cancel in-progress on new push for fast feedback.
concurrency:
  group: tf-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  TF_VERSION: "1.8.0"
  AWS_REGION: "us-east-1"

jobs:

  # ─── 1. Validate — fmt + syntax for every stack ──────────────────────────
  validate:
    name: Validate (${{ matrix.stack }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        stack: [cluster, dev, prod]
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/tf-setup
        with:
          stack: ${{ matrix.stack }}
          aws-role-arn: ${{ secrets.AWS_CI_ROLE_ARN }}

      - uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: v0.50.3

      - name: tflint
        run: tflint --init && tflint -f compact
        working-directory: stacks/${{ matrix.stack }}

      - name: fmt check
        run: terraform fmt -check -recursive
        working-directory: stacks/${{ matrix.stack }}

      - name: validate
        run: terraform validate
        working-directory: stacks/${{ matrix.stack }}

  # ─── 2. IaC security scan (Trivy) ────────────────────────────────────────
  scan:
    name: IaC scan (Trivy)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Trivy — upload findings to Security tab
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: config
          scan-ref: .
          format: sarif
          output: trivy-iac.sarif
          severity: HIGH,CRITICAL

      - uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-iac.sarif

      # Gate blocks the PR on CRITICAL only. HIGH findings are still uploaded to
      # the Security tab (SARIF step above) for visibility, but don't block merges.
      # Accepted CRITICALs are suppressed in .trivyignore with justifications.
      - name: Trivy — gate (fails PR on CRITICAL)
        uses: aquasecurity/trivy-action@v0.35.0
        with:
          scan-type: config
          scan-ref: .
          format: table
          severity: CRITICAL
          exit-code: "1"
          trivyignores: .trivyignore

  # ─── 3. Plan — shows the diff to the reviewer as a PR comment ────────────
  plan:
    name: Plan (${{ matrix.stack }})
    runs-on: ubuntu-latest
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        stack: [cluster, dev, prod]
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/tf-setup
        with:
          stack: ${{ matrix.stack }}
          aws-role-arn: ${{ secrets.AWS_CI_ROLE_ARN }}

      - name: terraform plan
        id: plan
        run: |
          set -o pipefail
          terraform plan -no-color -out=tfplan 2>&1 | tee plan.txt
          terraform show -json tfplan > plan.json
        working-directory: stacks/${{ matrix.stack }}

      - name: Create comment body
        if: always()
        run: |
          ICON="${{ steps.plan.outcome == 'success' && '✅' || '❌' }}"
          echo "## ${ICON} Terraform Plan — \`${{ matrix.stack }}\`" > comment.md
          echo "<details><summary>Show Plan</summary>" >> comment.md
          echo "" >> comment.md
          echo '```hcl' >> comment.md
          tail -c 60000 stacks/${{ matrix.stack }}/plan.txt >> comment.md
          echo '```' >> comment.md
          echo "</details>" >> comment.md

      - name: Post plan as PR comment
        if: always()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: plan-${{ matrix.stack }}
          path: comment.md

      # Cost estimate — fully self-contained. INFRACOST_SKIP_UPLOAD stops the push
      # to Infracost Cloud, so there is NO external "Infracost" status check; the
      # API key is still used locally for pricing. The estimate is published as a
      # table in the job summary and a sticky PR comment. Informational only
      # (continue-on-error so a cost-tooling hiccup never blocks an infra merge).
      - name: Setup Infracost
        if: steps.plan.outcome == 'success'
        uses: infracost/actions/setup@v3
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - name: Infracost estimate
        if: steps.plan.outcome == 'success'
        continue-on-error: true
        env:
          INFRACOST_SKIP_UPLOAD: "true"
        run: |
          infracost breakdown --path stacks/${{ matrix.stack }}/plan.json --format table | tee infracost.txt
          {
            echo "## 💰 Infracost estimate — \`${{ matrix.stack }}\`"
            echo ""
            echo '```'
            cat infracost.txt
            echo '```'
          } | tee cost.md >> "$GITHUB_STEP_SUMMARY"

      - name: Post cost as PR comment
        if: steps.plan.outcome == 'success'
        continue-on-error: true
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: cost-${{ matrix.stack }}
          path: cost.md

  # ─── 4. PR gate — single status check for branch protection ──────────────
  iac-checks:
    name: iac-checks
    if: always()
    needs: [validate, scan, plan]
    runs-on: ubuntu-latest
    steps:
      - name: Gate
        run: |
          if ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}; then
            echo "One or more IaC checks failed."; exit 1
          fi
          echo "All IaC checks passed."
